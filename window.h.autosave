#ifndef WINDOW_H
#define WINDOW_H


class Window{

private:
        sf::RenderWindow window_;
public:
    Window(int W, int H) : window;
};

class Player : public sf::Sprite{
public:
    Player(const sf::Vector2f &position) : sf::Sprite(){
        setPosition(position);
    };
    void loadTextures();
};

#endif // WINDOW_H
